# 2026-05-28

- Started treating context as a system of record instead of retyping it every session.
- Stood up this brain repo: stable facts, conventions, decisions, cheatsheet, and a
  dated timeline the assistant reads before answering.
- Measured the effect. Setup tokens per session went from ~10,000 to ~2,500, about a
  75% cut, and the daily catch-up time mostly disappeared.
- Next: move the core tables off Global scope to the `x_ptb_*` convention, and look at
  putting this brain on an automated memory layer so the read and cleanup loop runs
  without manual upkeep.
