# 2026-04-28

- Fixed a server-side block that was sitting in the wrong function, which made the
  shift-report data come back undefined on load. Added the missing client-side binding.
- Standardized completion logic on `u_status === 'completed'`. The legacy
  `u_completed` boolean is deprecated and unreliable.
- Wellness popup now fires on a 30-second poll, so no page refresh is needed.
- Consolidated everything into one update set, ~340 records, confirmed isolated despite
  the "Global" scope label.
