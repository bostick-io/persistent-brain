# 2025-01-15

- Added an `app_user.role` field and gated user management behind `admin`.
- Standardized task state on `status === 'done'`. The legacy `is_complete` boolean is deprecated and unreliable.
- Daily digest now runs on the shared scheduler instead of its own timer.
- Next: move the digest template into the repo so it stops living only in the database.
